# Secrets
.env

# Python
__pycache__/
*.pyc
.venv/
venv/
*.egg-info/

# Pipeline outputs (drafts + confirmation artifacts shouldn't be committed)
output/drafts/*
!output/drafts/.gitkeep

# Local tooling state
ruvector.db

# Local Airtable mock runtime state
# (data/mock_airtable_seed.json IS committed on purpose -- fake starter
# records; only the runtime file the mock client reads/writes is ignored)
data/mock_airtable.json

# OS
.DS_Store

# IDE
.vscode/
.idea/
